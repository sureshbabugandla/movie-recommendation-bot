# CineBot — Python dependencies
# Tested on Python 3.10–3.12

tensorflow-cpu>=2.15      # deep-learning intent model (use 'tensorflow' if you have a GPU)
scikit-learn>=1.3         # TF-IDF, LinearSVC baseline, metrics, splits
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12

# Only needed to run the notebook locally (already present on Colab):
jupyter
notebook
